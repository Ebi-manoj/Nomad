export enum TaskType {
  PICKUP = 'PICKUP',
  DROPOFF = 'DROPOFF',
}

export enum TaskStatus {
  PENDING = 'PENDING',
  IN_PROGRESS = 'IN_PROGRESS',
  COMPLETED = 'COMPLETED',
  CANCELLED = 'CANCELLED',
}
